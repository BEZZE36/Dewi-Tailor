rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Gallery folder: allow anyone to read, only authenticated users to write
    match /gallery/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
